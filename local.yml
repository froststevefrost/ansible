---
- name: Initial bootstrap
  hosts: localhost
  connection: local
  become: true

  tasks:

    - name: Install packages.
      package:
        name: 
          - htop
          - tmux
          - vim-nox
          - neovim
          - curl
          - wget 
          - mlocate
        state: present
